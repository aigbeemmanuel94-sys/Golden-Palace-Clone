kind = "web"
previewPath = "/"
title = "Gold Palace Jewelry"
version = "1.0.0"
id = "artifacts/gold-palace"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20221

[services.development]
run = "pnpm --filter @workspace/gold-palace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/gold-palace", "run", "build" ]
publicDir = "artifacts/gold-palace/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20221"
BASE_PATH = "/"
